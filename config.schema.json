{
  "pluginAlias": "Molekule",
  "pluginType": "platform",
  "singular": false,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "homebridge-molekule",
        "required": true
      },
      "email": {
        "title": "Email",
        "type": "string",
        "format": "email",
        "placeholder": "example@example.com",
        "required": true
      },
      "password": {
        "title": "Password",
        "type": "string",
        "placeholder": "********",
        "required": true,
        "description": "<b>Ensure password is correct, othersise a reset may become necessary.</b>"
      },
      "threshold": {
        "title": "Filter Change Warning Percentage",
        "type": "integer",
        "default": 10,
        "minimum": 0,
        "maximum": 100,
        "description": "Show <b>Change Filter</b> warning once Filter Life reaches this value."
      },
      "excludeAirMiniPlus": {
        "title": "Exclude Air Mini+",
        "type": "boolean",
        "default": false,
        "description": "Exclude HomeKit enabled Air Mini+"
      },
      "silentAuto": {
        "title": "Silent Auto",
        "type": "boolean",
        "default": false,
        "description": "When setting Auto/Manual state, use Silent or Standard auto state on Air Pro devices?"
      },
      "quietMode": {
        "title": "Expose Quiet switch",
        "type": "boolean",
        "default": false,
        "description": "Add a 'Quiet' switch for Air Pro devices. Turning it on enables Auto + Quiet (silent) mode."
      },
      "co2Threshold": {
        "title": "CO₂ Alarm Threshold (ppm)",
        "type": "integer",
        "default": 1000,
        "minimum": 400,
        "maximum": 5000,
        "description": "CO₂ level (ppm) above which the CO₂ sensor reports an abnormal/detected state."
      },
      "pollInterval": {
        "title": "Poll Interval (seconds)",
        "type": "integer",
        "default": 30,
        "minimum": 5,
        "maximum": 600,
        "description": "How often to refresh device state from the Molekule API. Changes made from HomeKit update instantly; this controls how quickly changes made elsewhere (physical controls, the Molekule app, air-quality drift) are reflected."
      }
    }
  }
}
